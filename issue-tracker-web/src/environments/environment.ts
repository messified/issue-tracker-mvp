export const environment = {
  production: false,
  apiUrl: 'http://localhost:8080/api',
  wsUrl: 'ws://localhost:8080/ws'  // WebSocket URL
};

// src/environments/environment.prod.ts
// export const environment = {
//   production: true,
//   apiUrl: '/api',
//   wsUrl: `ws://${window.location.host}/ws`  // Same host, ws protocol
// };